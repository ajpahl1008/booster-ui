Template.search.rendered =function searchOnCreated() {
  // var serverInfo;
  // if (Meteor.isClient) {
  //    Meteor.call('getEntirePatchingList', function(error, results) {
  //       serverInfo = results.data;
  //       console.log(serverInfo);
  //       Session.set('targetServer', serverInfo);
  //    });
  //
  // }

};


if (Meteor.isClient) {
  // Template.search.helpers({
  //     isChecked: function() {
  //
  //     },
  //     server_list: function(searchString) {
  //         Meteor.call('searchServerByName', function(error,results){
  //   //          serverInfo = results.data;
  //     //        console.log(serverInfo);
  //             Session.set('retrievedServers', results.data);
  //         });
  //     }
  // });

}
